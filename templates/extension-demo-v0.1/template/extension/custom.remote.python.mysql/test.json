{
    "activeGate": {
        "id": "{{.agid}}",
        "name": "{{.name}}"
    },
    "enabled": true,
    "endpointName": "{{.name}}",
    "extensionId": "custom.remote.python.mysql",
    "properties": {
        "connection_host": "{{.host}}",
        "connection_password": "secret",
        "connection_port": "3306",
        "connection_user": "demotest9"
    },
    "useGlobal": false
}